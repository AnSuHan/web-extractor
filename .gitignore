node_modules
dist
.DS_Store
*.local
.env
.env.*

# 빌드 산출물
*.tsbuildinfo
deploy/

# 추출 결과물 (수집한 사이트의 내용이 들어 있다 — 저장소에 올리지 않는다)
captures/

# 런처가 만드는 것
.browser-profile/
.tools/

# 확장 안으로 빌드되는 앱 (run.bat 이 자동 생성)
collector/app/

# egg-crawler: 서버가 만드는 수집물과 자격증명은 커밋하지 않는다
egg-crawler/site/
egg-crawler/site.tmp/
# .env 는 위에서 무시됨. 예시 파일은 커밋한다.
!egg-crawler/.env.example
