node_modules
dist
.DS_Store
*.local
.env
.env.*

# 빌드 산출물
*.tsbuildinfo
deploy/

# 추출 결과물 (수집한 사이트의 내용이 들어 있다 — 저장소에 올리지 않는다)
captures/

# 런처가 만드는 것
.browser-profile/
.tools/

# 확장 안으로 빌드되는 앱 (run.bat 이 자동 생성)
collector/app/
